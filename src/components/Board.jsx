const Board = ({ voidGame }) => {

    return (
        <div>
            <p>Board goes here</p>
            <button onClick={voidGame}>Back to menu</button>
        </div>
    )
}

export default Board;